#ifndef KU_PARSER_H
#define KU_PARSER_H

#include "source.h"

typedef struct Parser Parser;
typedef struct Statement Statement;

struct Parser {};

struct Statement {};

void parse_str(str s);
void parse_source(SourceText source);

#endif // KU_PARSER_H
